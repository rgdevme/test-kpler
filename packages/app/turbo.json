{
	"extends": ["//"],
	"tasks": {
		"build": {
			"dependsOn": ["$TURBO_EXTENDS$", "@access/server#generate:types"]
		},
		"check:typecheck": {
			"dependsOn": ["$TURBO_EXTENDS$", "@access/server#generate:types"]
		}
	}
}
